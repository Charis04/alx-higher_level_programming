#!/usr/bin/python3

def divisible_by_2(my_list=[]):
    atarashi_list = []
    if my_list:
        for num in my_list:
            atarashi.append(False if num % 2 else True)
        return atarashi_list

